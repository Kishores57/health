## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting and manipulation
react-day-picker | For the calendar date picker component
clsx | For conditional class names (utility)
tailwind-merge | For merging tailwind classes (utility)

## Notes
Integration with /api/tests, /api/bookings, and /api/reports
Admin dashboard requires authentication (handled via useAuth)
Using standard Lucide icons for medical theme
